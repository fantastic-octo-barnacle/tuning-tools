{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Tuning Studio extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/vscode"],
      "outFiles": ["${workspaceFolder}/vscode/dist/**/*.js"],
      "preLaunchTask": "Tuning Studio: build extension"
    }
  ]
}
